{
  "name": "dmn-product-card-test",
  "version": "1.0.0",
  "description": "Custom Shopify product card based on the Horizon Shopify theme",
  "scripts": {
    "tw:dev": "npx @tailwindcss/cli -i src/tailwind-input.css -o theme/assets/tailwind-output.css --watch",
    "tw:build": "npx @tailwindcss/cli -i src/tailwind-input.css -o theme/assets/tailwind-output.css --minify",
    "dev": "npm run tw:dev & shopify theme dev --path theme",
    "deploy": "npm run tw:build && shopify theme push --path theme"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/devgoncharuk/dmn-product-card-test.git"
  },
  "keywords": [
    "Shopify",
    "TailwindCSS"
  ],
  "author": "devgoncharuk",
  "license": "ISC",
  "dependencies": {
    "@tailwindcss/cli": "^4.3.0",
    "tailwindcss": "^4.3.0"
  }
}
